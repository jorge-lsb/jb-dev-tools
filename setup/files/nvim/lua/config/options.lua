-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Add any additional options here

-- Tell terminals that support it (kitty, iTerm2, WezTerm, Ghostty, Alacritty...)
-- to render the cursor using nvim's Cursor highlight colors, so the character
-- underneath always has contrast instead of blending into the background.
vim.opt.guicursor = "n-v-c-sm:block-Cursor,i-ci-ve:ver25-Cursor,r-cr-o:hor20-Cursor"
